<!DOCTYPE html>
<html>
    <body>
        <script>
           
/*
function convertMiliseg (dias, horas, minutos, segundos) {
return (((dias * 24 + horas) * 60  + minutos) * 60 + segundos) * 1000;
}

  const dias = prompt("Dias:");
  const horas = prompt("horas");
  const minutos = prompt("minutos:");
  const segundos = prompt("segundos:");

const resultado = convertMiliseg(
  parseInt(dias),
  parseInt(horas),
  parseInt(minutos),

  parseInt(segundos),
)

console.log(`resultado: $(resultado) milisegundos`);
 ``   
*/

/*
function sumaAsincrona (a, b, segundos) {
   console.log("cargando");
    return new Promise(
      resolve => {
        setTimeout(() => {
            resolve(a + b);
        }, segundos * 1000);
      }
      
    );
}

let puntos = "";
const intervalo = setInterval(() => {
   if (puntos.length >= 3) {
    puntos = "";
   } else {
    puntos += ".";
   }
   console.clear();
   console.log(`\rCargando ${puntos}`);
}, 1000);

sumaAsincrona(123, 14, 13).then(resultado =>{
    clearInterval(intervalo);
    console.clear();
  console.log(`resultado: ${resultado}`);
});
*/

/*
const proxBisiestos = (desde) => {
  let res = [];
  let año = desde + 1;
  
  while(res.length < 50) {
 if ((año % 4 === 0 && año % 100 !== 0) ||
(año  % 400 === 0)) res.push(año);
  año++;
}
  console.log(res);
};

proxBisiestos (2020)
*/

/*
function wordMark(texto) {
  const palabras = texto.split(' ');
  const maximaLongitud = 
  Math.max(...palabras.map(p => p.length));
  const borde = '*'.repeat(maximaLongitud + 4);

  console.log(borde);
  for (const palabra of palabras) {
    const space = ' '. repeat(maximaLongitud - palabra.length);
    console.log(`* ${palabra}${space} *`)
  }
  console.log(borde);
}

wordMark("hello nigga");
*/

/*
function biggestSecond(lista) {
  const uniqueNumbers = [...new Set(lista)];
  uniqueNumbers.sort((a, b) => b - a);
   if(uniqueNumbers.length < 2) {
    return null;
   }
   return uniqueNumbers[1];
}
//const numeros = [13, 45, 83, 10, 48];
//console.log(biggestSecond(numeros));

const input = prompt("introduce los numeros: ");
const numeros = input.split(" ").map(n => parseInt(n.trim()));

console.log("segundo mas grande:", biggestSecond(numeros));
alert(`el segundo numero mas grande es: ${biggestSecond(numeros)}`);


// node,js


const readline = require("readline");

function biggestSecond(lista) {
  const uniqueNumbers = [...new Set(lista)];
  uniqueNumbers.sort((a, b) => b - a);
   if(uniqueNumbers.length < 2) {
    return null;
   }
   return uniqueNumbers[1];
}
const rl = readline.createInterface({
  input: process.stdin,
  output: process.stdout
});

rl.question("introduce el numero separado por comas: ", (input) => {
  const numeros = input.split(",").map(n => parseInt(n.trim()));
  console.log("segundo numero mas grande:", biggestSecond(numeros));
  rl.close();
});
*/

/*
function cicloSexagenario(año) {
  const elementos = ["madera", "fuego", "tierra", "metal", "agua"];
  const animales = ["rata", "buey", "tigre", "conejo", "dragon", "serpiente", "caballo", "oveja", "mono", "gallo", "perro", "cerdo"];

const añoBase = 1984
const diferencia = año - añoBase;

const elementoIndex = Math.floor (((diferencia % 10 + 10)% 10) / 2);
  const animalesIndex = ((diferencia % 12) + 12) % 12;

  return `${elementos[elementoIndex]}
  ${animales[animalesIndex]}`;
  }

  for(let año = 1984; año <= 2043; año++) {
    console.log(`${año}: ${cicloSexagenario(año)}`);
}
*/

/*
function Anagrama(Txt1, Txt2) {
  const palabra1 = Txt1.toLowerCase();
  const palabra2 = Txt2.toLowerCase();
    if(palabra1 === palabra2) return false;
    if(palabra1.length !== palabra2.length) return false;

    const Pordenada1 = palabra1.split('').sort().join('');
    const Pordenada2 = palabra2. 
    
    split('').sort().join('');

    return Pordenada1 === Pordenada2;
}

const Txt1 = prompt("introduce la primera palabra:");
const Txt2 = prompt("Introduce la segunda palabra:");

if(Anagrama(Txt1, Txt2)) {
  alert(` "${Txt1}" y "${Txt2}" son anagramas.`);
} else {
  alert(`"${Txt1}" y "${Txt2}" no son anagramas hajahjuah maldito estupido :v`);
}
*/

/*
function conteodepalabras(Text) {
 let texto = Text.toLowerCase();
// convierte a minuscula
  let limpio = "";
  for (let i = 0; i < texto.length; i++) {
    const caracter = texto[i];

    if(
      (caracter >= 'a' &&  caracter <= 'z') || //letras
      (caracter >= '0' && caracter <= '9') || // numeros
      caracter === ' ' // espacios
    ) {
      limpio += caracter;
    }
  }  

// limpia los signos de puntuación
  const palabras = [];
  let palabraActual = "";
// separación de palabras
  for(let i = 0; i < limpio.length; i++) {
    const caracter = limpio[i];
    if (caracter !== ' ') {
    palabraActual += caracter;
  } else if (palabraActual !== "") {
    palabras.push(palabraActual);
    palabraActual = "";
  }
}

if (palabraActual !== "") {
  palabras.push(palabraActual)
}
// añade la ultima palabra si algo quedó

const contador = {};
for (let i = 0; i < palabras.length; i++) {
  const palabra = palabras[i];
  if (contador[palabra]) {
    contador[palabra]++;
  } else {
    contador[palabra] = 1;
  }
} // conteo de palabras

  for (let palabra in contador) {
  console.log(`"${palabra}" aparece ${contador[palabra]} veces`);
  }
}
 
conteodepalabras("hola hola; hola, mundo! muNdo mundo.,");
*/

/*
function pseudoAleatorio(seed) {
  const a = 1664525;
  const c = 1013904223;
  const m = 2 ** 32

  seed = (a * seed + c) % m ;
  return seed;
}
  function getPseudoNumeroAleatorio(min = 0, max = 100) {
    const seed = Date.now() % 100000;
    const raw = pseudoAleatorio(seed);
    const scaled = raw % (max - min + 1) + min;
    return scaled;
}
 console.log(getPseudoNumeroAleatorio());
*/


try { }
catch{}































        </script>
    </body>
</html>
